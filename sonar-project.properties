sonar.projectKey=Nexa-Inc25_job-hub-app
sonar.organization=job-hub-app

# Only scan actual source directories
sonar.sources=frontend/src,backend

# Exclude everything that shouldn't be analyzed
sonar.exclusions=**/node_modules/**,**/build/**,**/dist/**,**/uploads/**,**/coverage/**,**/*.min.js,**/*.bundle.js,**/public/pdf.worker.min.js,**/*.test.js,**/*.spec.js

# Skip test coverage for now (speeds up analysis)
sonar.coverage.exclusions=**/*

# Encoding
sonar.sourceEncoding=UTF-8
