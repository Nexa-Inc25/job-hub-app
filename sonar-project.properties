# SonarCloud Configuration
sonar.projectKey=Nexa-Inc25_job-hub-app
sonar.organization=nexa-inc25

# Project info
sonar.projectName=Job Hub App
sonar.projectVersion=1.0.0-pilot

# Source directories
sonar.sources=frontend/src,backend

# Exclusions (node_modules, build artifacts, etc.)
sonar.exclusions=**/node_modules/**,**/build/**,**/dist/**,**/uploads/**,**/*.test.js,**/*.spec.js,**/coverage/**

# Test files (if you add tests later)
sonar.tests=frontend/src,backend
sonar.test.inclusions=**/*.test.js,**/*.spec.js

# JavaScript/TypeScript settings
sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info,backend/coverage/lcov.info

# Encoding
sonar.sourceEncoding=UTF-8

