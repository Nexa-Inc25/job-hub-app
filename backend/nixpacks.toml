[phases.setup]
# Include system libraries required by canvas package
nixPkgs = ["nodejs_20", "pkg-config", "cairo", "pango", "libjpeg", "giflib", "librsvg", "pixman"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
